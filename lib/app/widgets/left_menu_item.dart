import 'package:flutter/material.dart';
class LeftMenuItem extends StatelessWidget {
  const LeftMenuItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
